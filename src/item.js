export class Item{
   constructor(id, x, y, width, height, content) {
    this.id = id;    
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.content = content;
  }
}